# Generated by Copilot
services:
  universal-downloader:
    restart: unless-stopped
    image: ghcr.io/longphamdev/universaldownloader:latest
    # build: .  # Uncomment this line if you want to build locally instead
    container_name: universal-downloader-app
    ports:
      - "1234:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s